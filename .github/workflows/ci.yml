name: Go Build and Release

on:
  push:
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Build
        run: go build -v ./...
      - name: Test
        run: go test -v ./...

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Build binaries
        run: |
          GOOS=linux GOARCH=amd64 go build -o jsonfilter-linux
          GOOS=windows GOARCH=amd64 go build -o jsonfilter.exe
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            terraform-ansible-inventory-linux
            terraform-ansible-inventory.exe
